`java.lang.String` was loaded by the bootstrap loader. source is
`shared objects file` the JDK's class-data archive

`Hello` was loaded by the application loader. source is
`file:/Users/brandon/java-bootcamp/examples/module-01-exercises/`