# Lab 13 - REST design notes

## Resources

- Customer collection: `/api/v1/customers`
- Customer item: `/api/v1/customers/{customerId}`
- Status sub-resource: `/api/v1/customers/{customerId}/status`

## URI table

| Resource | Method | URI | Success status |
| -------- | ------ | --- | -------------- |
| Collection list | GET | `/api/v1/customers` | 200 |
| Create | POST | `/api/v1/customers` | 201 + `Location` |
| Item | GET | `/api/v1/customers/{customerId}` | 200 |
| Replace | PUT | `/api/v1/customers/{customerId}` | 200 |
| Delete | DELETE | `/api/v1/customers/{customerId}` | 204 |
| Patch status | PATCH | `/api/v1/customers/{customerId}/status` | 200 |

## Method / status map

| Operation | Method | Success | Client errors |
| --- | --- | --- | --- |
| List | GET | 200 | 400 bad query (e.g. `size=500`) |
| Create | POST | 201 + `Location: /api/v1/customers/CUS-1003` | 400 validation, 409 duplicate `customerId` |
| Get by id | GET | 200 | 404 for `CUS-9999` |
| Replace | PUT | 200 | 400, 404 |
| Patch status | PATCH | 200 | 400 (value outside `ACTIVE`/`PROSPECT`), 404, 409 illegal transition |
| Delete | DELETE | 204 | 404 |

Safe = server state does not change. Idempotent = repeating the call leaves the same state as calling it once.

| Method | Safe | Idempotent |
| --- | --- | --- |
| GET | yes | yes |
| PUT | no | yes |
| DELETE | no | yes (second call is 404, state is the same) |
| PATCH status | no | yes here (setting ACTIVE twice is still ACTIVE) |
| POST | no | no (a retried timeout can create a second customer) |

## Version strategy

URI versioning, `/api/v1/...`. The version is visible in server logs, browser tabs, and curl output for free, and v1 and v2 can be served side by side during a transition. Header versioning (`Accept: application/vnd.northstar.v2+json`) hides the version from logs and makes manual testing clunky, so it was not chosen for this lab.

Breaking: renaming or removing a response field, changing its type, or adding a required request field. Non-breaking: adding an optional field or query parameter.

## Error contract

- Header: `X-Correlation-Id` (example `lab-request-001`); echoed in the body, generated by the server if absent
- Body fields: timestamp, status, error, message, path, correlationId
- One envelope for every error from every endpoint. Never include stack traces, raw SQL, internal hostnames, or another customer's data.

404 for `GET /api/v1/customers/CUS-9999`:

```json
{
  "timestamp": "2026-08-23T21:00:00Z",
  "status": 404,
  "error": "Not Found",
  "message": "Customer not found: CUS-9999",
  "path": "/api/v1/customers/CUS-9999",
  "correlationId": "lab-request-001"
}
```

## Pagination / filter / sort

- `page` (0-based, default 0), `size` (default 20, max 100), `status` (`ACTIVE` | `PROSPECT`), `sort` (`field,direction`, e.g. `fullName,asc`)
- Example list URL: `GET /api/v1/customers?status=ACTIVE&page=0&size=20&sort=fullName,asc`
- Page wrapper fields: content, page, size, totalElements
- With the fixtures, that URL returns Amina only (`totalElements: 1`); unfiltered returns Amina and Ravi.

## Fixtures

- `CUS-1001` Amina Khan ACTIVE
- `CUS-1002` Ravi Singh PROSPECT
- `CUS-9999` not found

## Stack reminder

REST / OpenAPI · Angular client later · PostgreSQL later · GitHub Actions later.
