package com.northstar.crm.dto;

/** Outbound API/service response shape — not the entity. */
public class CustomerResponse {
    // DONE: stub only — later: customerId, status, ... Keep DTO separate from entity.
    /*
    String customerId;
    CustomerStatus status;
    String fullName;
    Instant createdAt;
     */
}
