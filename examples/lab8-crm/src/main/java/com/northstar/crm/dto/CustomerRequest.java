package com.northstar.crm.dto;

/** Inbound create/update payload — not the entity. */
public class CustomerRequest {
    // DONE: stub only — later labs add fullName, email, etc. Do not import entity here incorrectly for Lab 8.
    /*
    String fullName;
    String email;
     */
}
